{
  "name": "codespan-wasm",
  "version": "0.1.0",
  "description": "WebAssembly bindings for the Rust codespan-reporting crate",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/*.js",
    "lib/*.d.ts",
    "pkg"
  ],
  "scripts": {
    "build": "wasm-pack build --target nodejs --out-name index --out-dir ./pkg && rm pkg/.gitignore && rm pkg/package.json && tsc -b",
    "package": "npm pack"
  },
  "dependencies": {
    "debug": "^4.3.4"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
